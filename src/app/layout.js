import './globals.css'

export const metadata = {
  title: 'Mask Cursor',
  description: 'mask cursor animation effect',
}

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  )
}
